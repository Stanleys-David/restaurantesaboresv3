import { getAllTables, updateTableStatus, auth, getUserProfile, onAuthStateChanged, signOut } from "./firebase.js"
const labels={available:"Disponible",occupied:"Ocupada",reserved:"Reservada",cuenta_solicitada:"Lista para pago",en_limpieza:"En limpieza"};const escape=(value)=>String(value??"").replace(/[&<>'"]/g,(c)=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[c]);let tables=[];let authorized=false;const tableClass=(status)=>status==="reserved"?"reserved":status==="occupied"?"occupied":status==="cuenta_solicitada"?"payment":"available";
function render(){const root=document.getElementById("waiterTables");root.innerHTML=tables.length?tables.map((table,index)=>{const pos=table.position||{x:10+(index%4)*23,y:12+Math.floor(index/4)*28};return `<button class="plan-table ${tableClass(table.status||"available")}" data-table="${table.id}" style="left:${pos.x}%;top:${pos.y}%"><strong>${escape(table.name)}</strong><small>${labels[table.status||"available"]}</small></button>`}).join(""):'<p class="empty-tables">No hay mesas configuradas.</p>'}
async function load(){const result=await getAllTables();tables=result.tables||[];render()}
function openTable(table){const modal=document.createElement("div");modal.className="modal";modal.style.display="flex";modal.innerHTML=`<div class="modal-content table-detail"><button class="close">&times;</button><h2>${escape(table.name)}</h2><p><strong>Estado:</strong> ${labels[table.status||"available"]}</p><p><strong>Capacidad:</strong> ${table.seats||0} puestos</p><div class="form-buttons"><button class="btn btn-primary" data-products>Agregar productos</button><button class="btn btn-success" data-finish>Solicitar finalizar mesa</button></div></div>`;document.body.appendChild(modal);modal.querySelector(".close").onclick=()=>modal.remove();modal.querySelector("[data-products]").onclick=()=>{localStorage.setItem("activeWaiterTable",JSON.stringify({id:table.id,name:table.name}));location.href="menu.html"};modal.querySelector("[data-finish]").onclick=async()=>{await updateTableStatus(table.id,"cuenta_solicitada",table.activeOrderId||null);modal.remove();load()}}
document.addEventListener("DOMContentLoaded",()=>{onAuthStateChanged(auth,async(firebaseUser)=>{if(!firebaseUser)return location.href="inicioSesion.html";const profile=await getUserProfile(firebaseUser.uid);if(!profile.success||!["admin","mesero"].includes(profile.user.role)){await signOut(auth);return location.href="inicioSesion.html"}authorized=true;load()});document.addEventListener("click",(event)=>{const node=event.target.closest("[data-table]");if(node&&authorized){const table=tables.find((item)=>item.id===node.dataset.table);if(table)openTable(table)}})})